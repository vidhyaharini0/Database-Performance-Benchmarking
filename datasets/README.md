# Dataset

This project uses a synthetic Course Management System dataset containing one million records generated using the Python Faker library.

The dataset is not included in this repository due to its size.

You can generate your own dataset using the provided scripts or adapt the benchmark to another dataset.
